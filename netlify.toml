[build]
  publish = "."    # publish from repository root
  command = ""     # no build step; static HTML

[context.production.environment]
  # Add environment variables here if needed (e.g., API keys)
